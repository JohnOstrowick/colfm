<!-- sample_button_setup.html -->
<pre><code>// colfm.cpp around line 210:
void onSampleButton();

// colfm.cpp around line 251:
QAction *actSampleButton{};

// toolbars.h where buttons are added:
actSampleButton = tb->addAction(getIcon("sample.png"), "Sample Button");
actSampleButton->setToolTip("Click me for something");

// toolbars.h where connects are added:
connect(actSampleButton, &QAction::triggered, this, &ColFM::onSampleButton);

// sample.h:
#ifndef SAMPLE_H
#define SAMPLE_H

#include &lt;QMessageBox&gt;

inline void ColFM::onSampleButton() {
    statusBar()-&gt;showMessage("Sample clicked", 2000);
}

#endif // SAMPLE_H

// colfm.cpp after class definition:
#include "sample.h"

Newer code embeds the icon in the code itself. When you compile you have to use that otherwise the app depends on finding ./icons/ with the icons in it. Suggest you use that incantation instead. 

viewMenu->addAction(IconsData::getIcon("eye.png"), "Show Hidden Files", [this] {
	    onToggleHidden();
	});
</code></pre>
