Declare function in colfm.cpp (e.g. vi +210):

void onSettings();


Add button pointer in colfm.cpp (e.g. vi +240):

QAction *treeBtn{}, *columnBtn{}, *iconBtn{}, *toggleHiddenBtn{}, *settingsBtn{};


Define visual button in toolbars.h:

settingsBtn = tb->addAction(QIcon("icons/settings.png"), "Settings");
settingsBtn->setToolTip("Open Settings dialog");


Wire slot in toolbars.h:

connect(settingsBtn, &QAction::triggered, this, &ColFM::onSettings);


Define stub:

inline void ColFM::onSettings() {
    statusBar()->showMessage("TODO: Open Settings Dialog", 2000);
}
